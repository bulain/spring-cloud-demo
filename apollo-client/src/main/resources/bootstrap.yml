server:
  port: 8101
spring:
  application:
    name: apollo-client
app:
  id: apollo-client
apollo:
  meta: http://localhost:9080
  bootstrap:
    namespaces: application.yml
    enabled: true
    eagerLoad:
      enabled: true
    
