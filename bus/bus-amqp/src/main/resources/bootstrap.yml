spring:
  application:
    name: bus-amqp
  cloud:
    discovery:
      enabled: true
      serviceId: config-server
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
