server:
  port: 8774
spring:
  application:
    name: zipkin-client
  cloud:
    discovery:
      enabled: true
      serviceId: config-server
  zipkin:
    base-url: http://localhost:8773

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/