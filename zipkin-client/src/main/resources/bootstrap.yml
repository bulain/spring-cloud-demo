server:
  port: 8774
spring:
  application:
    name: zipkin-client
  zipkin:
    base-url: http://localhost:8773
  cloud:
    discovery:
      enabled: true
      serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/