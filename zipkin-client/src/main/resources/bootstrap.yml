spring:
  application:
    name: zipkin-client
  zipkin:
    base-url: http://localhost:8773
  sleuth:
    sampler:
      percentage: 1.0
  cloud:
    discovery:
      enabled: true
      serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/