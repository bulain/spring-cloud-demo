server:
  port: 8762
spring:
  application:
    name: consul-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: YAML
        watch:
          enabled: true
